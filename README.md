# AgriYield_Predictor
Production-ready crop yield (Tons/Ha) forecasting app. Random Forest (R² 0.98) is trained on a cleaned/merged dataset (preventing data leakage) and deployed via a Flask API. Full ML pipeline included.
